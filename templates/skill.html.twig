{% extends 'base.html.twig' %}


{% block body %}
<main>
        <div class="container">
            <h2 class="title margin_title edit">{{categorie.name}}</h2>
            <div class="paragraph_dark margin_title edit">{{categorie.text|raw}}</div>
            <ul class="column_list">
            {% for skill in skills %}
                <li class="list_style_on padding edit">{{skill.name}}</li>
            {% endfor %}
            </ul>
        </div>
    </main>
{% endblock %}

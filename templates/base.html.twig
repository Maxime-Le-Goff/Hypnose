<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{asset('assets/css/style.css')}}">
        <script src="{{asset('assets/js/app.js')}}" defer></script>
        {% if app.user %}
        {% if 'ROLE_ADMIN' in app.user.roles %}
        <script src="{{asset('assets/js/edit.js')}}" defer></script>
        {% endif %}
        {% endif %}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&display=swap" rel="stylesheet">
        <script src="https://kit.fontawesome.com/8d63125858.js" crossorigin="anonymous"></script>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    </head>
    <body>

    <header>
        <div class="hamburger">
            <i class="fa-solid fa-bars fa-xl"></i>
        </div>
        <nav class="nav-bar">
            <ul>
                <li><a class="main_nav active" href="{{path('app_home')}}">Accueil</a></li>
                <li id="dropdown_li"><a class="main_nav drop" href="#">Compétences <i class="fa-solid fa-chevron-down"></i></a>
                <ul class="dropdown">
                {% for categorie in categories %}
                    <li><a href="{{path('app_skill', {id : categorie.id})}}">{{categorie.name}}</a></li>
                {% endfor %}
                </ul></li>
                <li><a class="main_nav" href="{{path('app_tarifs')}}">Tarifs</a></li>
                <li><a class="main_nav" href="{{path('app_avis')}}">Avis</a></li>
                <li><a class="main_nav" href="{{path('app_rdv')}}">Rendez-vous</a></li>
                {% if app.user %}
                <li><a class="main_nav" href="{{path('app_logout')}}">Se déconnecter</a></li>
                {% else %}
                 <li><a class="main_nav" href="{{path('app_login')}}">Se connecter</a></li>
                {% endif %}
            </ul>
        </nav>
    </header>
    {% block body %}{% endblock %}
    <hr>
    <footer>
        <nav> 
            <ul>
                <li><a class="main_nav" href="#">Laissez un avis</a></li>
                <li><a class="main_nav" href="#">Mentions légales</a></li>
                <li><a class="main_nav" href="#">Contact</a></li>
            </ul>
        </nav>
    </footer>
</body>
</html>
        
    </body>
</html>

{% extends 'base.html.twig' %}

{% block body %}
 <main>
        <div class="container">
            <h2 class="title edit">{{data[0].new_avis_title}}</h2>
            <p class="text_align_center paragraph_dark edit">{{data[0].new_avis_text}}</p>
            <form action="POST">
                <textarea name="textarea" class="field" id="textarea" rows="15" placeholder="Votre Avis"></textarea>
                <div class="btn">Envoyer</div>
            </form>
        </div>
    </main>
{% endblock %}